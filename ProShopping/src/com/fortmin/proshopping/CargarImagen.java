package com.fortmin.proshopping;

import android.app.Activity;
import android.os.Bundle;

public class CargarImagen extends Activity {

	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		/*
		 * Drawable drawable =
		 * getResources().getDrawable(R.drawable.img_tvled32); Bitmap bitmap =
		 * ((BitmapDrawable) drawable).getBitmap(); ByteArrayOutputStream stream
		 * = new ByteArrayOutputStream();
		 * bitmap.compress(Bitmap.CompressFormat.PNG, 100, stream); byte[]
		 * bitMapData = stream.toByteArray();
		 * 
		 * Imagen imagen = new Imagen(); Imagen imgcodif =
		 * imagen.encodeImagen(bitMapData);
		 * imagen.setImagen(imgcodif.getImagen());
		 * 
		 * Nube nube = new Nube(GestionNube.OPE_CARGAR_IMAGEN);
		 * nube.ejecutarCargarImagen("TodoMusica", "tvled32", imagen);
		 */
	}

}
